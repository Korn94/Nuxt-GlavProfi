// tsconfig.json
{
  "extends": "./.nuxt/tsconfig.json",
  "include": [
    "**/*.ts",
    "**/*.tsx",
    "**/*.vue",
    "stores/**/*.ts"
  ],
  "exclude": ["node_modules", ".nuxt", "dist", ".git", ".output"],
  "compilerOptions": {
    "types": ["nuxt", "vite/client"],
    "baseUrl": ".",
    "ignoreDeprecations": "6.0", // игнорируем предеупреждении о том что baseUrl устаревает в TS 7.0
    "paths": {
      "~/*": ["./app/*"],
      "@/*": ["./app/*"],
      "~~/*": ["./"],
      "@@/*": ["./"],
    }
  }
}
